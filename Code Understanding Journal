Exercise 1:
The main components involved are; 
                                1.cli.py - which is the user interface of the program, which translates commands to taskmanager calls
                                2.app,py - this is the manager components that orchestrates operations between cli.py and storage.py
The execution flow when creating/updating a task: 
                                                cli.py -> app.py -> models.py -> storage.py
How data is stored and retrieved: 
                                * The program has a file database that save/load tasks to a JSON
                                * The app.py component manages the tasks if it's going to be stored or retrieved
                                * data is stored in memory dictionary; with the task id and the task
                                * The the task dictionary is written in the JSON file, the task id is returned to cli.py for easy retrieval
Interesting designs: the storage class teaches JSON how to convert objects(task,timestamp), enum values to primitive types 

Execise 2:
My initial thought of the priority of a task;
                                            A task has a scale of priority; 1-low to 4-urgent
                                            This is displayed with '!' to symbolise the task's priority
What I have discovered;
                      The reason of using enum values instead of primitive data types is because of fixed value and also easy access
                      A deep undertsanding of the connection of the components(files)
The key insights the guided questions helped you uncover: 
                                                        How the tasks are stored in a JSON file, that when storing a task that does not have priority the default is 2.
                                                        Using enum values is a safety procedure
I did not have any misconceptions other an learning more, that the program keeps track of how many priority types are there, e.g. "5 low priority".

Exercise 3:

The flow diagram:

The changes that occur during task completion: 
                                              * This change will update in three places of placement; status variable, complete_at varialble and the update_at variable.
                                              * This will change the status from in progress to done, in the task manager to the storage file
                                              * The time of complition will also be capture, by the complete_at variable and the update_at variable will take it storage to update everything
Potential points of failure:
                            * When the user uses an invalid update status string, the one that is not in these choices: {"todo", "in_progress", "review", "done"}
                            * When the user enters a wrong/invalid task id
                            * all in all when updating all the string passed should be valid else that will lead to point of failure

Persisting the changes: 
                      * By safe protection coding, to update a status you can only use these four choices only ["todo", "in_progress", "review", "done"]
                      * By using constants enum values for safety
                      * And if the user command is not clear the program will crash


Task priority sorting and filtering algorithm (Python)

AI’s explanation change your understanding of the algorithm:
                                                            * AI gave me more insight about the algorithm, gave me explanation about thing I did not take into consideration

I do not have any aspects were still difficult to understand after the AI explanation.

Explaining to another Junior developer:
                                      Imagine you're managing a todo list and need to know which task to work on next. Instead of just looking at priority,
                                      this algorithm scores each task by combining multiple factors like how urgent the deadline is, whether it's already done,
                                      and if it has a "blocker" tag. Think of it like a weighted voting system where different factors contribute different amounts
                                      to a final score. The code multiplies priority by 10 to give it weight, adds bonuses for tasks due soon or recently updated, 
                                      and subtracts points for completed work so you don't waste time re-prioritizing done tasks. Then it sorts everything by that 
                                      final score, highest first.
Testing against AI:
                    Yes, I did test my understanding by answering the AI suggested questions and also explaining some thing to AI that it did not cover
Improving the algorithm:
                        The current code assumes every task has an updated_at timestamp, but what if it's None? This would crash with a typeError.
                        Set {days_since_update = float('inf')} if the timestamp doesn't exist, which treats unknown tasks and prevents crashing.
